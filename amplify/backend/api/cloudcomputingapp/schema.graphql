type NoteData
@model
@auth(rules: [{allow: owner, operations: [read, create, update, delete], provider: userPools}])
@aws_cognito_user_pools
{
    id: ID!
    name: String!
    description: String
    image: String
}